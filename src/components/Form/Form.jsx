import React from 'react';
// import PropTypes from 'prop-types';

const Form = () => (
  <form>
    <input type="number"/>
    <input type="number"/>
    <input type="submit"/>
  </form>
)

export default Form;